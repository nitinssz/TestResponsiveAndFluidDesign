---
// Hero component with custom fluid scaling
---

<div class="hero">
  <div class="hero-container">
    <div class="hero-content">
      <h1 class="hero-title">Responsive & Fluid Design</h1>
      <p class="hero-description">
        Experience the perfect blend of responsive breakpoints and fluid scaling. 
        This design adapts seamlessly across all devices with precise control.
      </p>
      <div class="hero-buttons">
        <a href="#" class="btn btn-primary">Get Started</a>
        <a href="#" class="btn btn-secondary">Learn More</a>
      </div>
    </div>
    <div class="hero-image">
      <div class="image-placeholder">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 400 300" fill="none">
          <rect width="400" height="300" fill="rgba(255, 255, 255, 0.1)" rx="12"/>
          <circle cx="150" cy="100" r="40" fill="rgba(255, 255, 255, 0.2)"/>
          <path d="M50 220 L120 150 L180 180 L350 80" stroke="rgba(255, 255, 255, 0.3)" stroke-width="8" stroke-linecap="round" stroke-linejoin="round"/>
          <rect x="160" y="180" width="200" height="90" fill="rgba(255, 255, 255, 0.15)" rx="8"/>
        </svg>
        <p class="placeholder-text">Image Placeholder</p>
      </div>
    </div>
  </div>
</div>


<style>
  .hero {
    min-height: 100vh;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    /* 320px-425px: 1rem, stays at 1rem */
    padding: 1rem;
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 30% 20%, rgba(255, 255, 255, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 70% 80%, rgba(255, 255, 255, 0.05) 0%, transparent 50%);
    pointer-events: none;
  }

  .hero-container {
    max-width: 100%; /* Default: full width */
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr;
    /* 320px-425px: 1.5rem to 2.5rem */
    gap: clamp(1.5rem, 1.2rem + 0.9375vw, 2.5rem);
    align-items: center;
    position: relative;
    z-index: 2;
    width: 100%;
  }

  .hero-content {
    text-align: center;
    color: white;
  }

  .hero-image {
    display: none;
  }

  .image-placeholder {
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
  }

  .image-placeholder svg {
    width: 100%;
    height: auto;
    max-width: 400px;
    filter: drop-shadow(0 10px 30px rgba(0, 0, 0, 0.2));
  }

  .placeholder-text {
    color: rgba(255, 255, 255, 0.6);
    font-size: 0.9rem;
    font-weight: 500;
    margin: 0;
  }

  .hero-title {
    /* 320px-425px: 1.75rem to 2.8rem */
    font-size: clamp(1.75rem, 0.75rem + 3.125vw, 2.8rem);
    font-weight: 700;
    line-height: 1.2;
    /* 320px-425px: 0.75rem to 1.4rem */
    margin-bottom: clamp(0.75rem, 0.25rem + 1.5625vw, 1.4rem);
    background: linear-gradient(45deg, #fff, #f0f0f0);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-description {
    /* 320px-425px: 0.9rem to 1.25rem */
    font-size: clamp(0.9rem, 0.7875rem + 0.3516vw, 1.25rem);
    line-height: 1.6;
    /* 320px-425px: 1.25rem to 2rem */
    margin-bottom: clamp(1.25rem, 0.875rem + 1.1719vw, 2rem);
    opacity: 0.9;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .hero-buttons {
    display: flex;
    flex-wrap: wrap;
    /* 320px-425px: 0.75rem to 1.2rem */
    gap: clamp(0.75rem, 0.6rem + 0.4688vw, 1.2rem);
    align-items: center;
    justify-content: center;
    max-width: 100%;
  }

  .btn {
    /* 320px-425px: 0.6rem to 1.1rem vertical padding */
    padding: clamp(0.6rem, 0.4375rem + 0.5078vw, 1.1rem) clamp(1.2rem, 0.875rem + 1.0156vw, 2.2rem);
    border: none;
    border-radius: 0.5rem;
    /* 320px-425px: 0.8rem to 1.05rem */
    font-size: clamp(0.8rem, 0.7219rem + 0.2441vw, 1.05rem);
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    text-decoration: none;
    display: inline-block;
    text-align: center;
    white-space: nowrap;
    /* Fixed width - scales from 138px to 175px */
    width: clamp(138px, 103.125px + 10.9375vw, 175px);
    flex-shrink: 0;
  }

  .btn-primary {
    background: linear-gradient(45deg, #ff6b6b, #ee5a24);
    color: white;
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(255, 107, 107, 0.3);
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.1);
    color: white;
    border: 2px solid rgba(255, 255, 255, 0.3);
  }

  .btn-secondary:hover {
    background: rgba(255, 255, 255, 0.2);
    border-color: rgba(255, 255, 255, 0.5);
    transform: translateY(-2px);
  }

  /* 425px-768px: STATIC content with increasing margins */
  @media (min-width: 425px) and (max-width: 767px) {
    .hero {
      /* Fluid padding from 1rem to 3rem */
      padding: clamp(1rem, -0.0441rem + 2.4559vw, 3rem);
    }
    
    .hero-container {
      /* Lock content to 425px width, margins grow */
      max-width: 425px;
      gap: 2.5rem;
    }
    
    .hero-title {
      font-size: 2.8rem;
      margin-bottom: 1.4rem;
    }
    
    .hero-description {
      font-size: 1.25rem;
      margin-bottom: 2rem;
    }
    
    .hero-buttons {
      gap: 1.2rem;
    }
    
    .btn {
      padding: 1.1rem 2.2rem;
      font-size: 1.05rem;
      width: 175px;
    }
  }

  /* 768px-1024px: FLUID scaling (tablet) */
  @media (min-width: 768px) and (max-width: 1023px) {
    .hero {
      padding: 3rem;
    }
    
    .hero-title {
      font-size: clamp(3rem, 2.25rem + 0.98vw, 4rem);
      margin-bottom: clamp(1.5rem, 1.25rem + 0.326vw, 2rem);
    }
    
    .hero-description {
      font-size: clamp(1.3rem, 1.222rem + 0.102vw, 1.5rem);
      margin-bottom: clamp(2rem, 1.75rem + 0.326vw, 2.5rem);
    }
    
    .hero-buttons {
      gap: clamp(1.5rem, 1.25rem + 0.326vw, 2rem);
    }
    
    .btn {
      padding: clamp(1.2rem, 1.068rem + 0.172vw, 1.4rem) clamp(2.4rem, 2.136rem + 0.344vw, 2.8rem);
      font-size: clamp(1.1rem, 1.035rem + 0.085vw, 1.2rem);
      width: clamp(180px, 163.75px + 2.122vw, 200px);
    }
    
    .hero-container {
      gap: clamp(3rem, 2.5rem + 0.652vw, 4rem);
      grid-template-columns: 1fr 1fr;
      max-width: 1200px;
    }
    
    .hero-content {
      text-align: left;
    }
    
    .hero-buttons {
      justify-content: flex-start;
    }

    .hero-image {
      display: flex;
      align-items: center;
      justify-content: center;
    }
  }

  /* 1024px-1280px: STATIC content with increasing margins */
  @media (min-width: 1024px) and (max-width: 1279px) {
    .hero {
      /* Fluid padding from 3rem to 5rem */
      padding: clamp(3rem, 1.125rem + 1.953vw, 5rem);
    }
    
    .hero-container {
      /* Lock content to 1024px width, margins grow */
      max-width: 1024px;
      gap: 4rem;
      grid-template-columns: 1fr 1fr;
    }
    
    .hero-title {
      font-size: 4rem;
      margin-bottom: 2rem;
    }
    
    .hero-description {
      font-size: 1.5rem;
      margin-bottom: 2.5rem;
    }
    
    .hero-buttons {
      gap: 2rem;
    }
    
    .btn {
      padding: 1.4rem 2.8rem;
      font-size: 1.2rem;
      width: 200px;
    }
    
    .hero-content {
      text-align: left;
    }
    
    .hero-buttons {
      justify-content: flex-start;
    }

    .hero-image {
      display: flex;
      align-items: center;
      justify-content: center;
    }
  }

  /* 1280px-1440px: FLUID scaling (large desktop) */
  @media (min-width: 1280px) and (max-width: 1439px) {
    .hero {
      padding: 5rem;
    }
    
    .hero-title {
      font-size: clamp(4rem, 3.5rem + 0.391vw, 5rem);
      margin-bottom: clamp(2rem, 1.75rem + 0.195vw, 2.5rem);
    }
    
    .hero-description {
      font-size: clamp(1.5rem, 1.45rem + 0.039vw, 1.6rem);
      margin-bottom: clamp(2.5rem, 2.25rem + 0.195vw, 3rem);
    }
    
    .hero-buttons {
      gap: clamp(2rem, 1.75rem + 0.195vw, 2.5rem);
    }
    
    .btn {
      padding: clamp(1.4rem, 1.3rem + 0.078vw, 1.5rem) clamp(2.8rem, 2.6rem + 0.156vw, 3rem);
      font-size: clamp(1.2rem, 1.15rem + 0.039vw, 1.3rem);
      width: clamp(200px, 187.5px + 0.977vw, 220px);
    }
    
    .hero-container {
      gap: clamp(4rem, 3.5rem + 0.391vw, 5rem);
      grid-template-columns: 1fr 1fr;
      max-width: 1400px;
    }
    
    .hero-content {
      text-align: left;
    }
    
    .hero-buttons {
      justify-content: flex-start;
    }

    .hero-image {
      display: flex;
      align-items: center;
      justify-content: center;
    }
  }

  /* 1440px+: STATIC content with increasing margins */
  @media (min-width: 1440px) {
    .hero {
      /* Fluid padding from 5rem to 8rem (up to 1920px) */
      padding: clamp(5rem, 2rem + 2.083vw, 8rem);
    }
    
    .hero-container {
      /* Lock content to 1440px width, margins grow */
      max-width: 1440px;
      gap: 5rem;
      grid-template-columns: 1fr 1fr;
    }
    
    .hero-title {
      font-size: 5rem;
      margin-bottom: 2.5rem;
    }
    
    .hero-description {
      font-size: 1.6rem;
      margin-bottom: 3rem;
    }
    
    .hero-buttons {
      gap: 2.5rem;
    }
    
    .btn {
      padding: 1.5rem 3rem;
      font-size: 1.3rem;
      width: 220px;
    }
    
    .hero-content {
      text-align: left;
    }
    
    .hero-buttons {
      justify-content: flex-start;
    }

    .hero-image {
      display: flex;
      align-items: center;
      justify-content: center;
    }
  }
</style>